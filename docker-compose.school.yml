services:
  school:
    build:
      context: .
      dockerfile: Dockerfile.school
    ports:
      - "80:80"
    environment:
      - API_SERVER_HOST=${API_SERVER_HOST}
      - API_SERVER_PORT=${API_SERVER_PORT:-8080}
